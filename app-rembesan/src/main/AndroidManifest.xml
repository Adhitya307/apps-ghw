<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Untuk Android 10+ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KERJAPRAKTIK"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:label"
        tools:targetApi="31">

        <!-- FileProvider untuk sharing file -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Semua Activity Rembesan -->
        <activity
            android:name="com.apps.ghw.rembesan.HistoryActivity"
            android:exported="false" />
        <activity
            android:name="com.apps.ghw.rembesan.InputData2Activity"
            android:exported="false" />
        <activity
            android:name="com.apps.ghw.rembesan.InputDataActivity"
            android:exported="false" />
        <activity
            android:name="com.apps.ghw.rembesan.HomeActivity"
            android:exported="false" />
        <activity
            android:name="com.apps.ghw.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name="com.apps.ghw.rembesan.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity DamBody -->
        <activity
            android:name="com.example.app_dambody.HistoryDamBodyActivity"
            android:exported="true"
            android:label="Riwayat Dam Body"
            tools:replace="android:exported,android:label" />

    </application>

</manifest>