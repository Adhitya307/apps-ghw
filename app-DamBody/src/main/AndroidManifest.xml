<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.app_dambody">

    <application
        android:label="@string/app_name"
        tools:replace="android:label"
        tools:node="merge">

        <!-- Activity utama Dam Body -->
        <activity
            android:name=".DamBodyHomeActivity"
            android:exported="false"
            tools:node="merge" />

        <activity
            android:name=".InputdataElv625"
            android:exported="false"
            tools:node="merge" />

        <activity
            android:name=".InputdataElv600"
            android:exported="false"
            tools:node="merge" />

        <!-- ✅ History Activity -->
        <activity
            android:name=".HistoryDamBodyActivity"
            android:exported="true"
            android:label="Riwayat Dam Body"
            tools:replace="android:exported,android:label"
            tools:node="merge" />

    </application>
</manifest>
